Ww saw if we pass n-- it sticks in the infinite look for printing number as the value n is passesd and then incremented

for function where we need to pass and extra counter we can use helper function and pass that offset value in the helper function

we saw in the sum of digits , count zeros in number example
